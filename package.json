{
  "name": "battle-ball",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/create-worker-adapter.mjs",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "lint": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^22.19.19",
    "@types/three": "^0.185.1",
    "tsx": "^4.22.1",
    "typescript": "^5.9.3",
    "vite": "^8.0.13"
  }
}
